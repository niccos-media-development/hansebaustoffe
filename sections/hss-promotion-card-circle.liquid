<div class="hss-promo-carousel">
  <div class="hss-promo-carousel-holder" id="carousel-{{ section.id }}">
    {% for block in section.blocks %}
      <a href="{{ block.settings.url }}" class="hss-promo-item">
        <div class="hss-promo-image {% if block.settings.highlight %}hss-promo-highlight{% endif %}">
          {% if block.settings.image %}
            <img src="{{ block.settings.image | img_url: '200x' }}" alt="{{ block.settings.heading }}">
          {% else %}
            <div class="hss-placeholder">+</div>
          {% endif %}
        </div>
        {% if block.settings.heading != blank %}
          <span class="hss-promo-title">{{ block.settings.heading }}</span>
        {% endif %}
      </a>
    {% endfor %}
  </div>
</div>

<style>
  .hss-promo-carousel {
    background: {{ section.settings.background_color }};
    padding: 20px 0;
    display: flex;
    justify-content: center;
    align-items: center;
  }

  .hss-promo-carousel-holder {
    display: flex;
    justify-content: center;
    align-items: center;
    gap: {{ section.settings.desktop_distance }}px;
    overflow-x: auto;
    scroll-behavior: smooth;
    white-space: nowrap;
    position: relative;
    max-width: 100%;
    padding: 10px;
  }

  .hss-promo-carousel-holder::-webkit-scrollbar {
    display: none;
  }

  .hss-promo-item {
    text-align: center;
    text-decoration: none;
    font-size: {{ section.settings.fontsize }}px;
    font-weight: 600;
    color: {{ section.settings.text_color }};
    font-family: {{ section.settings.font.family }}, sans-serif;
    flex: 0 0 auto;
  }

  .hss-promo-image {
    width: {{ section.settings.desktop_size }}px;
    height: {{ section.settings.desktop_size }}px;
    border-radius: {{ section.settings.border_radius }}px;
    overflow: hidden;
    position: relative;
    display: flex;
    align-items: center;
    justify-content: center;
    background-color: {{ section.settings.icon_background }};
  }

  .hss-promo-image img {
    width: 80%;
    height: auto;
    object-fit: cover;
  }

  .hss-placeholder {
    width: 100%;
    height: 100%;
    display: flex;
    align-items: center;
    justify-content: center;
    font-size: 20px;
    color: #ccc;
    background: #f5f5f5;
    border-radius: 50%;
  }

  .hss-promo-highlight::after {
    content: '';
    border: 2px solid {{ section.settings.highlight_color }};
    border-radius: {{ section.settings.border_radius }}px;
    width: 110%;
    height: 110%;
    position: absolute;
    top: -5%;
    left: -5%;
  }

  .hss-promo-title {
    display: block;
    margin-top: 5px;
  }

  @media only screen and (max-width: 768px) {
    .hss-promo-carousel-holder {
      justify-content: flex-start;
      padding: 10px 0;
      gap: {{ section.settings.mobile_distance }}%;
    }

    .hss-promo-item {
      flex: 0 0 auto;
      margin: 0 auto;
    }

    .hss-promo-image {
      width: {{ section.settings.mobile_size }}px;
      height: {{ section.settings.mobile_size }}px;
    }
  }
</style>

<script>
  document.addEventListener("DOMContentLoaded", function() {
    const container = document.querySelector("#carousel-{{ section.id }}");
    let scrollAmount = 0;
    let autoScroll;

    function startAutoScroll() {
      if ({{ section.settings.auto_rotate }}) {
        autoScroll = setInterval(() => {
          if (scrollAmount >= container.scrollWidth - container.clientWidth) {
            scrollAmount = 0;
          } else {
            scrollAmount += container.clientWidth / {{ section.settings.items_per_view }};
          }
          container.scrollTo({ left: scrollAmount, behavior: "smooth" });
        }, {{ section.settings.auto_rotate_speed }} * 1000);
      }
    }

    function stopAutoScroll() {
      clearInterval(autoScroll);
    }

    container.addEventListener("mouseenter", stopAutoScroll);
    container.addEventListener("mouseleave", startAutoScroll);

    startAutoScroll();
  });
</script>

{% schema %}
{
  "name": "HSS Promotion Card Circle",
  "settings": [
    {
      "type": "color",
      "id": "background_color",
      "default": "#f2f2f2",
      "label": "Hintergrundfarbe"
    },
    {
      "type": "color",
      "id": "icon_background",
      "default": "#ffffff",
      "label": "Hintergrundfarbe der Icons"
    },
    {
      "type": "range",
      "id": "desktop_size",
      "min": 50,
      "max": 150,
      "step": 1,
      "default": 92,
      "label": "Größe der Icons (Desktop, px)"
    },
    {
      "type": "range",
      "id": "desktop_distance",
      "min": 0,
      "max": 60,
      "step": 1,
      "default": 9,
      "label": "Abstand zwischen Icons (Desktop, px)"
    },
    {
      "type": "range",
      "id": "mobile_size",
      "min": 50,
      "max": 150,
      "step": 1,
      "default": 80,
      "label": "Größe der Icons (Mobile, px)"
    },
    {
      "type": "range",
      "id": "mobile_distance",
      "min": 0,
      "max": 10,
      "step": 1,
      "default": 2,
      "label": "Abstand zwischen Icons (Mobile, %)"
    },
    {
      "type": "checkbox",
      "id": "auto_rotate",
      "label": "Auto-Rotation aktivieren",
      "default": true
    },
    {
      "type": "range",
      "id": "auto_rotate_speed",
      "min": 1,
      "max": 10,
      "step": 1,
      "default": 3,
      "label": "Auto-Rotation Geschwindigkeit (Sekunden)"
    },
    {
      "type": "range",
      "id": "items_per_view",
      "min": 1,
      "max": 6,
      "step": 1,
      "default": 4,
      "label": "Sichtbare Icons"
    }
  ],
  "blocks": [
    {
      "type": "category",
      "name": "Kategorie",
      "settings": [
        {
          "type": "image_picker",
          "id": "image",
          "label": "Bild"
        },
        {
          "type": "text",
          "id": "heading",
          "default": "Kategorie",
          "label": "Titel"
        },
        {
          "type": "url",
          "id": "url",
          "label": "Link"
        },
        {
          "type": "checkbox",
          "id": "highlight",
          "label": "Hervorheben"
        }
      ]
    }
  ],
  "presets": [
    {
      "name": "HSS Promotion Card Circle",
      "blocks": [
        { "type": "category" },
        { "type": "category" },
        { "type": "category" }
      ]
    }
  ]
}
{% endschema %}
