{%- layout none -%}
{%- paginate collection.products by 4200000 -%}
  {%- for product in collection.products -%}
    {%- for variant in product.variants -%}
      {{ variant | json }}
    {%- endfor -%}
  {%- endfor -%}
{%- endpaginate -%}
